# CDC 화면 개선 방향

| 항목 | 내용 |
|---|---|
| 대상 | Cerebro ETL — CDC 파이프라인 / 연결정보 / 처리 로그 |
| 근거 | 「CDC 화면 UI/UX 및 기능 개선 분석」을 실제 소스코드·DB 스키마·실행 중 API에 대조 검증 |
| 작성일 | 2026-08-04 |
| 총 작업량 | **재산정 필요** (기존 항목별 추정 합계는 41인일, 단계별 합계는 42인일로 서로 불일치) |

---

## 1. 결론 요약

> **개선 분석의 문제 인식은 타당하다. 다만 P0로 분류된 핵심 항목(Lag·마지막 처리 시각)은
> 백엔드가 이미 값을 갖고 있어 화면 연결만 하면 된다.**
> 반대로 가장 큰 기능 갭인 다중 테이블 선택은 접근 방식을 바꿔야 스키마 변경을 피할 수 있다.

### 2026-08-13 구현 대조 재검토

- 0단계의 지표 연결은 현재 API·응답 타입이 모두 존재하므로 그대로 우선 구현한다.
- `consumerLag`는 타깃 DB 반영 완료 건수가 아니라 **Kafka sink consumer의 offset 기반 추정치**다.
  화면과 문서에서 이를 단순한 E2E 지연으로 표현하지 않는다.
- 상태 SSOT 결정은 화면 개선의 선행 조건이다. 다중 테이블·마법사보다 먼저 Kafka Connect
  실측 상태와 메타데이터 상태의 불일치 표시를 완성한다.
- 정합성 검증의 무조건적인 `COUNT(*)`는 운영 소스 DB에 큰 부하를 줄 수 있다. PK 범위·샘플·
  DB 통계 기반 검증 정책과 실행 시간 제한을 먼저 설계한 뒤 후순위로 구현한다.
- DLQ 재처리는 중복·순서 역전·삭제 이벤트 재적용 위험이 있으므로 단순 재발행 기능으로 만들지
  않는다. 멱등성 키와 승인·감사 이력이 설계된 뒤에 착수한다.
- 다중 테이블을 `POST /api/pipelines` N회 호출로 구현하면 중간 실패 시 일부만 생성된다.
  배치 API와 보상 삭제 또는 서버 트랜잭션 경계를 먼저 제공해야 한다.

---

## 2. 검증 결과

### 2-1. 이미 구현되어 있는 것

**지표** — `GET /api/metrics/daily-load/realtime`이 아래 값을 이미 반환한다.

| 필드 | 개선 분석이 요구한 항목 |
|---|---|
| `consumerLag` | **1-1-1 Lag 미표시** |
| `lastProgressAt` | **1-1-2 마지막 처리 시각 미표시** |
| `throughputPerSecond` | 처리율 |
| `estimatedRecoverySeconds` | **분석에 없던 항목 — 지연 해소 예상 시간** |
| `endOffset` / `committedOffset` | Lag 산출 근거 |
| `collectionStatus` | 수집 정상 여부 |

**카탈로그 조회 API** — 다중 테이블 선택에 그대로 재사용할 수 있다.

| API | 용도 |
|---|---|
| `GET /api/connections/{id}/schemas` | 스키마 목록 |
| `GET /api/connections/{id}/tables` | 테이블 목록 |
| `POST /api/connections/{id}/test` | 연결 테스트 |

**연계 정리** — 이미 자동화되어 있다.

| 서비스 | 하는 일 |
|---|---|
| `PostgresReplicationCleanupService` | 파이프라인 삭제 시 publication·replication slot 자동 정리 |
| `KafkaTopicCleanupService` | 토픽 정리 |
| `KafkaPipelineStateSynchronizer` | 커넥터 상태 주기 동기화 |

### 2-2. 실제로 없는 것

| 항목 | 확인 내용 |
|---|---|
| 다중 테이블 | `PipelineCreateRequest.sourceTable`이 **단일 문자열** |
| 스냅샷 모드 | 커넥터 템플릿에 `snapshot.mode` **미설정** → Debezium 기본값 |
| DLQ | `errors.deadletterqueue` **미설정** |
| 운영/테스트 분류 | 필드 없음 |
| 사전요건 자동 점검 | 없음 |

---

## 3. 개선 항목 판정

| # | 항목 | 판정 | 작업량 | 설계 |
|---|---|---|---:|---|
| 1 | Lag·마지막 처리 시각 목록 표시 | ✅ 연결 | 1.0 | 5-1 |
| 2 | 소스→타깃 방향성 2행 표기 | ✅ 연결 | 0.5 | 5-1 |
| 5 | 연결정보 등록 폼에 연결 테스트 | 🟢 가능 | 1.0 | 5-6 |
| 6 | 연결정보 사용처 표시 | 🟢 가능 | 1.5 | 5-6 |
| 7 | 연결 사전요건 자동 점검 | 🟢 가능 | 3.0 | 5-7 |
| 8 | 스냅샷 모드 선택 | 🟢 가능 | 2.0 | 5-4 |
| 9 | 컬럼 제외·마스킹 | 🟢 가능 | 3.0 | 5-4 |
| 10 | **다중 테이블 선택 + 패턴 매칭** | 🟡 조건부 | 11.0 | 5-4 |
| 11 | 좌측 계층 트리 | 🟢 가능 | 4.0 | 5-5 |
| 12 | 처리 로그 Lag 추이·자동 갱신 | 🟢 가능 | 3.0 | 5-8 |
| 13 | 타깃 실측 count 정합성 검증 | 🟢 가능 | 3.0 | 5-9 |
| 14 | DLQ 조회·재처리 | 🟡 조건부 | 8.0 | 5-10 |

---

## 4. 단계별 실행 계획

| 순서 | 단계 | 항목 | 완료 조건 |
|---:|---|---|---|
| 1 | **0단계 — 즉시 가시성** | Lag·마지막 처리 시각 / 방향성 2행 / 20초 갱신 | 목록에서 문제 파이프라인 식별 가능 |
| 2 | **1단계 — 상태 신뢰성** | Kafka Connect 실측 상태 SSOT / 메타데이터·Airflow 요청과 불일치 경고 | 상태 출처와 불일치가 명확함 |
| 3 | **2단계 — 입력 안전장치** | 신규·기존 연결 테스트 / 사용처 표시·삭제 보호 / CDC 사전요건 점검 | 잘못된 연결로 생성되는 사고 차단 |
| 4 | **3단계 — 생성 모델 확장** | 스냅샷 모드 / 컬럼 정책 모델 / 배치 생성 API / 다중 테이블 마법사 | 부분 생성 없이 여러 테이블 생성 가능 |
| 5 | **4단계 — 관측성 확장** | Lag 시계열 저장·그래프 / 자동 갱신 / 계층 탐색 | 추세와 대규모 목록 탐색 가능 |
| 6 | **5단계 — 데이터 보증** | 부하 제한 정합성 검증 / DLQ 격리·승인·멱등 재처리 | 운영 DB와 데이터 중복 위험 통제 |

> 기존 인일 추정은 항목 합계와 단계 합계가 일치하지 않고, 배치 API·감사 이력·부하 제한 설계가
> 빠져 있으므로 일정 기준으로 사용하지 않는다. 각 단계 착수 시 API·DB migration·테스트를 포함해
> 다시 산정한다.

---

# 5. 화면 설계

---

## 5-1. 파이프라인 목록 — 컬럼 재구성

**화면 위치**: `CDC → 파이프라인` 목록 테이블

**기능**: 목록만 보고 **"지금 조치가 필요한 파이프라인이 어느 것인가"** 를 판단할 수 있게 한다.

### 화면 구성

```
┌──────────────────────────────────────────────────────────────────────────────────┐
│ [이름 검색] [상태 ▾] [유형 ▾] [분류 ▾]                          [+ 신규 생성]     │
├────┬──────────────┬────────────────────────────┬────────┬─────────┬──────────────┤
│ ☐  │ 이름          │ 소스 → 타깃                 │ 상태   │ 지연     │ 마지막 처리   │
├────┼──────────────┼────────────────────────────┼────────┼─────────┼──────────────┤
│ ☐  │ LW_IMP018M   │ ORACLE · CSB.TB_IMP018M    │ 실행 중 │ 78,546  │ 12분 전       │
│    │              │ → PG · public.lw_imp018m   │        │ ⚠ 약 12분│              │
├────┼──────────────┼────────────────────────────┼────────┼─────────┼──────────────┤
│ ☐  │ app-log      │ Filebeat · /var/log/app    │ 실행대기│ —       │ —            │
│    │              │ → PG · log_landing.app_log │        │         │              │
└────┴──────────────┴────────────────────────────┴────────┴─────────┴──────────────┘
```

### 컬럼 정의

| 컬럼 | 내용 | 표시 규칙 |
|---|---|---|
| ☐ | 다중 선택 | 일괄 삭제·내보내기용 (제어는 제외) |
| 이름 | 파이프라인명 | 클릭 시 상세 Drawer |
| **소스 → 타깃** | 2행 표기 | 1행: `DB유형 · 스키마.테이블` / 2행: `→ DB유형 · 스키마.테이블` |
| 상태 | 상태 배지 | 5-2 참조 |
| **지연** | `consumerLag` | 임계 초과 시 ⚠ + 색상 강조. 2행에 `estimatedRecoverySeconds` 병기 |
| **마지막 처리** | `lastProgressAt` | **상대 시각** (`12분 전`). 툴팁에 절대 시각 |
| 관리 | 상세·삭제 | 컬럼명을 `제어` → **`관리`** 로 변경 (제어는 Airflow 담당) |

### 조회 방식

| 항목 | 내용 |
|---|---|
| 목록 | `GET /api/pipelines` |
| 지표 | `GET /api/metrics/daily-load/realtime` — 파이프라인 ID로 조인 |
| 갱신 주기 | **20초** (기존 실시간 지표 주기와 동일) |
| 기본 정렬 | **상태 심각도 → 지연 내림차순** (문제 있는 항목이 항상 위) |
| 페이지 크기 | 20건. 1페이지일 때 페이지네이션 숨김 |

---

## 5-4. 파이프라인 생성 마법사

**화면 위치**: `CDC → 파이프라인 → [+ 신규 생성]` — 기존 단일 모달을 **5단계 마법사**로 교체

**기능**: 단계별 검증을 거쳐, 완료 시 즉시 실행 가능한 상태가 되도록 한다.

### 단계 구성

```
①기본정보 → ②연결 → ③대상선택 → ④실행옵션 → 검토 → 완료
```

#### ① 기본 정보

| 필드 | 형식 | 검증 |
|---|---|---|
| 파이프라인명 | 텍스트 | 중복 검사, 명명 규칙 안내 |
| 유형 | 라디오 (테이블 CDC / 로그파일 적재) | |
| 분류 | 라디오 (운영 / 테스트) | **신규 필드** |
| 설명 | 텍스트 | 선택 |

#### ② 연결

| 필드 | 형식 |
|---|---|
| 소스 연결 | 드롭다운 (`GET /api/connections`) |
| 타깃 연결 | 드롭다운 |
| | **[연결 테스트]** 버튼 — 통과해야 다음 단계 진행 |

```
소스 연결  [oracle-source        ▾]   ✅ 연결 성공 (0.4초)
타깃 연결  [target-db-postgres   ▾]   ✅ 연결 성공 (0.1초)
```

#### ③ 대상 선택 — 핵심 화면

```
┌─────────────────────────────────────────────────────────────────────┐
│ 스키마  [CSB ▾]                                                      │
│                                                                     │
│ ○ 개별 선택    ● 패턴 입력                                           │
│   패턴  [TB_IMP%          ]   → 18개 테이블 매칭                     │
│                                                                     │
│ ┌───┬────────────────┬──────────────────┬────────┬──────────────┐  │
│ │ ☑ │ 소스 테이블     │ 타깃 테이블       │ PK     │ 상태          │  │
│ ├───┼────────────────┼──────────────────┼────────┼──────────────┤  │
│ │ ☑ │ TB_IMP018M     │ lw_imp018m       │ IMP_NO │ ✅            │  │
│ │ ☑ │ TB_IMP073L     │ lw_imp073l       │ SEQ    │ ✅            │  │
│ │ ☐ │ TB_IMP090T     │ lw_imp090t       │ —      │ ⚠ PK 없음     │  │
│ └───┴────────────────┴──────────────────┴────────┴──────────────┘  │
│                                                                     │
│ 타깃 명명 규칙  [소문자 변환 ▾] [접두어: lw_  ] [접미어:     ]        │
│ 컬럼 설정      [선택한 테이블 컬럼 보기]  (제외·마스킹 지정)          │
└─────────────────────────────────────────────────────────────────────┘
```

| 요소 | 동작 |
|---|---|
| 스키마 드롭다운 | `GET /api/connections/{id}/schemas` |
| 테이블 목록 | `GET /api/connections/{id}/tables?schema=CSB` |
| 패턴 입력 | 입력 즉시 클라이언트 필터링 → 매칭 수 실시간 표시 |
| **PK 컬럼** | PK 없는 테이블은 **자동 체크 해제 + 선택 불가** (CDC 불가) |
| 타깃 명명 규칙 | 규칙 변경 시 타깃 테이블명 일괄 재계산 |
| 컬럼 설정 | 별도 모달 — 컬럼 목록에서 제외·마스킹 체크 |

#### ④ 실행 옵션

| 필드 | 선택지 | 기본값 |
|---|---|---|
| **스냅샷 모드** | 초기 적재 후 CDC / CDC만 / 스키마만 | 초기 적재 후 CDC |
| DELETE 반영 | 체크박스 | 해제 |
| Topic Prefix | 텍스트 (자동 생성값 제공) | `cdc-{파이프라인명}` |

#### 검토

```
아래 리소스가 생성됩니다.

  파이프라인    17건
  Kafka 토픽    17개
  커넥터        34개 (소스 17 + 싱크 17)
  타깃 테이블   17개 (자동 생성)

  ⚠ TB_IMP090T는 기본키가 없어 제외되었습니다.
```

#### 완료

```
✅ 17건이 실행대기 상태로 저장되었습니다.
   실행은 AirFlow에서 진행하십시오.

   [파이프라인 목록]  [AirFlow에서 열기]
```

### 조회 방식

| 단계 | API |
|---|---|
| ② | `POST /api/connections/{id}/test` |
| ③ | `GET /api/connections/{id}/schemas`, `GET /api/connections/{id}/tables` |
| 생성 | 신규 배치 생성 API 사용. `POST /api/pipelines` × N 클라이언트 반복 호출은 부분 실패 때문에 사용하지 않음 |

---

## 5-5. 좌측 계층 트리

**화면 위치**: `CDC → 파이프라인` 좌측 (기존 목록을 3분할 레이아웃으로 변경)

**기능**: 파이프라인이 10건을 넘으면 목록만으로는 찾기 어렵다. 소스 연결 기준으로 묶어 보여준다.

### 화면 구성

```
┌───────────────┬────────────────────────────┬──────────────┐
│ [전체][지연]  │  파이프라인 목록            │  속성 패널    │
│ [오류][대기]  │  (5-1 테이블)              │  (선택 시)    │
│ [검색      ]  │                            │              │
│               │                            │  기본 정보    │
│ ▼ oracle-src  │                            │  대상 테이블  │
│   ▼ CSB  (12) │                            │  연결 DAG     │
│     TB_IMP018M│                            │              │
│     TB_IMP073L│                            │              │
│ ▼ taran-db (5)│                            │              │
│ ▶ 로그파일 (3)│                            │              │
└───────────────┴────────────────────────────┴──────────────┘
```

| 요소 | 동작 |
|---|---|
| 트리 축 | **소스 연결 → 스키마 → 파이프라인** 3단 |
| 노드 배지 | 하위 항목 수 + 상태 아이콘 (하위 최악 상태를 롤업) |
| 상단 칩 | `전체 / 실행대기 / 실행 중 / 지연 / 오류 / 중지` — 트리·목록 동시 필터 |
| 검색 | 파이프라인명·테이블명 부분 일치, 매칭 노드까지 자동 펼침 |
| 양방향 동기화 | 트리 클릭 → 목록 필터링 / 목록 선택 → 트리 노드 하이라이트 |

> **우클릭 메뉴에 실행 제어를 넣지 않는다.** 제공 항목: `상세 보기`, `설정 수정`, `복제`, `삭제`, `AirFlow에서 열기`

---

## 5-6. 연결정보 화면

**화면 위치**: `CDC → 연결정보`

### 목록 컬럼 추가

| 컬럼 | 내용 | 조회 |
|---|---|---|
| **마지막 테스트** | 테스트 일시 + 결과 | `pipeline_connection` 컬럼 (기존 보유) |
| **사용처** | `CDC 2건 / ETL 5건` | 파이프라인·잡 참조 수 집계 (신규 쿼리) |

```
┌──────────────────┬──────────┬──────────────────┬─────────┬──────────────┬──────────┐
│ 이름              │ DB 유형   │ 접속 정보         │ 사용처   │ 마지막 테스트 │ 관리      │
├──────────────────┼──────────┼──────────────────┼─────────┼──────────────┼──────────┤
│ oracle-source    │ ORACLE   │ 192.168.50.91    │ CDC 12건 │ ✅ 2시간 전   │ 상세 수정 │
│ target-db-postgres│ POSTGRES │ target-db:5432   │ CDC 5건  │ ✅ 5분 전     │ 상세 수정 │
│                  │          │                  │ ETL 3건  │              │          │
└──────────────────┴──────────┴──────────────────┴─────────┴──────────────┴──────────┘
```

**사용처가 0건이 아니면 삭제 버튼 비활성화**하고, 클릭 시 참조 목록을 보여준다.

### 등록 폼에 연결 테스트 추가

```
┌────────────────────────────────────────┐
│ 연결명    [                         ]  │
│ DB 유형   [ORACLE ▾]                   │
│ Host      [                         ]  │
│ Port      [1521                     ]  │
│ ...                                    │
│                                        │
│        [연결 테스트]                    │
│        ✅ 연결 성공 (0.4초)             │
│                                        │
│                    [취소]  [저장]       │  ← 테스트 성공 전 비활성
└────────────────────────────────────────┘
```

| 항목 | 내용 |
|---|---|
| 조회 | `POST /api/connections/{id}/test` (신규는 임시 검증 엔드포인트 필요) |
| 저장 버튼 | **테스트 성공 전까지 비활성** |

---

## 5-7. 연결 사전요건 자동 점검

**화면 위치**: `CDC → 연결정보` 상세 화면 내 탭 또는 테스트 결과 하단

**기능**: 연결 성공 여부만이 아니라 **CDC가 가능한 상태인지**까지 점검한다.

### 화면 구성

```
CDC 사전요건 점검                                    [다시 점검]

Oracle 소스
  ✅ ARCHIVELOG 모드 활성화
  ✅ 데이터베이스 최소 보충 로깅
  ⚠ 테이블별 보충 로깅 — 12개 중 3개 미설정      [대상 보기]
  ✅ LogMiner 권한 보유
  ✅ C## 공통 사용자

                                    3개 항목 조치 필요 →  [해결 방법]
```

### 점검 항목

| DB | 점검 | 조회 SQL |
|---|---|---|
| Oracle | ARCHIVELOG | `SELECT log_mode FROM v$database` |
| Oracle | 최소 보충 로깅 | `SELECT supplemental_log_data_min FROM v$database` |
| Oracle | 테이블별 보충 로깅 | `SELECT ... FROM dba_log_groups` |
| Oracle | `C##` 공통 사용자 | 계정명 접두어 확인 |
| PostgreSQL | `wal_level` | `SHOW wal_level` |
| PostgreSQL | 복제 권한 | `SELECT rolreplication FROM pg_roles` |
| PostgreSQL | 슬롯 여유 | `SHOW max_replication_slots` |

**조회 SQL은 설치 매뉴얼에 정리된 내용을 그대로 재사용한다.**
미충족 항목의 `[해결 방법]`은 설치 매뉴얼 해당 절로 연결한다.

---

## 5-8. 처리 로그 화면

**화면 위치**: `CDC → 처리 로그`

### 화면 구성

```
┌─────────────────────────────────────────────────────────────────┐
│ [1시간] [6시간] [24시간] [7일]   [파이프라인 ▾]   자동갱신 [30초▾]│
├─────────────────────────────────────────────────────────────────┤
│  지연 추이                                                       │
│  80k ┤                                    ╭─────                │
│  40k ┤                        ╭───────────╯                     │
│    0 ┼────────────────────────╯                                 │
│      14:00      14:30      15:00      15:30                     │
├─────────────────────────────────────────────────────────────────┤
│ 처리 이력 │ 오류·상태 이력                                       │
│ ...                                                             │
└─────────────────────────────────────────────────────────────────┘
```

| 요소 | 내용 |
|---|---|
| **지연 추이 그래프** | 목록 상단에 배치. 증가 추세인지 해소 중인지 판단 |
| 기간 프리셋 | **기본 24시간** (현재 7일 → 변경) |
| 자동 갱신 | off / 10초 / 30초 / 60초 |
| 오류 이력 탭 | 1분 버킷 집계 외에 **개별 이벤트 원문 조회(펼침)** 지원 |

### 조회 방식

| 항목 | API |
|---|---|
| 처리 이력 | `GET /api/cdc/logs/processing?from=&to=&pipelineId=` |
| 오류 이력 | `GET /api/cdc/logs/events?from=&to=` |
| 지연 추이 | `GET /api/metrics/daily-load/hourly` 또는 realtime 이력 저장 필요 |

---

## 5-9. 정합성 검증 (타깃 실측 count)

**화면 위치**: `CDC → 파이프라인` 상세 Drawer → `정합성` 탭 (신규)

**기능**: "적재했다고 기록되어 있으나 실제로 존재하는가"를 확인한다.

### 화면 구성

```
정합성 검증                                        [지금 검증]

  마지막 검증   2026-08-04 15:20
  소스 건수     1,000,000
  타깃 건수     1,000,000        ✅ 일치

  이력
  ┌────────────────┬───────────┬───────────┬────────┐
  │ 검증 일시       │ 소스       │ 타깃       │ 결과   │
  ├────────────────┼───────────┼───────────┼────────┤
  │ 08-04 15:20    │ 1,000,000 │ 1,000,000 │ ✅     │
  │ 08-03 15:20    │   980,412 │   980,400 │ ⚠ -12  │
  └────────────────┴───────────┴───────────┴────────┘
```

| 항목 | 내용 |
|---|---|
| 실행 방식 | 버튼 클릭(수동) + Airflow 감시 DAG(주기) 양쪽 지원 |
| 불일치 시 | 경고 배지 + 대시보드 조치 대기열로 전달 |
| **ETL과 공통 로직으로 설계** | 두 화면의 판정이 달라지지 않도록 |

> 이 기능이 있었다면 **Debezium 트랜잭션 ID 재사용으로 이벤트 10만 건이 전량 스킵된 사고**가
> 즉시 드러났다. 커넥터는 `RUNNING`이었고 오류 로그도 없었다.

---

## 5-10. DLQ 조회·재처리

**화면 위치**: `CDC → 처리 로그` 내 `실패 데이터` 탭 (신규)

**전제**: 커넥터 설정에 `errors.deadletterqueue.topic.name` 추가 필요 (현재 미설정)

### 화면 구성

```
실패 데이터                                    [선택 항목 재처리]

┌───┬──────────────┬──────────────┬────────────────────────┬─────────┐
│ ☐ │ 발생 일시     │ 파이프라인    │ 오류 메시지             │ 원문    │
├───┼──────────────┼──────────────┼────────────────────────┼─────────┤
│ ☑ │ 08-04 14:02  │ LW_IMP018M   │ ORA-12954: 용량 초과    │ [보기]  │
│ ☐ │ 08-04 14:02  │ LW_IMP018M   │ ORA-12954: 용량 초과    │ [보기]  │
└───┴──────────────┴──────────────┴────────────────────────┴─────────┘
```

| 항목 | 내용 |
|---|---|
| 조회 | DLQ 토픽 consume (신규 API) |
| 원문 보기 | 이벤트 JSON 전문 모달 |
| 재처리 | 선택 항목을 원본 토픽으로 재발행 |

---

## 6. 상태 SSOT — 착수 전 결정

실행 제어를 Airflow로 위임하면서 **상태가 세 곳에 존재**하게 되었다.

| 출처 | 값의 성격 |
|---|---|
| Kafka Connect | 커넥터의 **실제** 상태 |
| Airflow | **마지막 제어 요청**의 결과 |
| 메타데이터 DB | 화면에 저장된 상태 값 |

### 권고

| 원칙 | 내용 |
|---|---|
| 화면 표시 기준 | **항상 Kafka Connect 실측값** |
| Airflow 값 | `최근 제어: 중지 요청 (08-04 14:05, 성공)` 형태로 **별도 병기** |
| 불일치 시 | **경고 배지 노출** (5-2) |

### `실행대기` 정의

| 방식 | 장단점 |
|---|---|
| A. 설정만 저장 | 설정 오류를 실행 시점에야 발견 |
| **B. 등록 후 PAUSED** | **저장 시점에 유효성 검증 가능.** `실행대기`가 PAUSED와 1:1 대응 |

**권고: B안.**

---

## 7. 착수 전 확인 사항

| # | 확인 사항 | 이유 |
|---|---|---|
| 1 | 다중 테이블 A안/B안 결정 | 마법사 설계 전체가 종속 |
| 3 | 정합성 검증을 ETL과 공통화할지 | 따로 만들면 판정이 달라짐 |
| 4 | 파이프라인 삭제 시 연결된 DAG 처리 | 삭제·비활성 중 선택 |
| 5 | 실행 중 수정 허용 여부 | 허용 시 커넥터 재시작이 필요하며 Airflow 경유 |

---

## 8. 참고 — 현재 성능 실측치

| 경로 | 100만건 소요 | 처리량 | 병목 |
|---|---:|---:|---|
| PostgreSQL → PostgreSQL | 455.9초 | 2,193건/초 | 소스(논리복제) |
| Oracle → PostgreSQL | 683.9초 | 1,462건/초 | 소스(LogMiner) |
| PostgreSQL → Oracle | 933.2초 | 1,072건/초 | 타깃(JDBC 싱크) |
| Oracle → 원격 Oracle | (10만건) 286.3초 | 349건/초 | 타깃(원격·공유 XE) |

다중 테이블 도입 시 커넥터 수가 늘면 이 수치가 낮아질 수 있으므로 **2단계 적용 후 재측정**을 권한다.

---

## 9. 이번 정리에서 제외한 항목

| 항목 | 제외 사유 |
|---|---|
| 스키마 변경(DDL) 정책 분기 | 상태 기계가 하나 더 생긴다. **운영 데이터가 쌓여 실제 발생 빈도를 확인한 뒤** 결정할 것을 권한다 |
