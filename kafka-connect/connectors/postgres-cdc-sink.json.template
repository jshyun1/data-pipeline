{
  "connector.class": "io.debezium.connector.jdbc.JdbcSinkConnector",
  "tasks.max": "1",
  "topics": "${ORACLE_CUSTOMERS_TOPIC}",
  "connection.url": "jdbc:postgresql://${TARGET_DB_HOST}:${TARGET_DB_PORT}/${TARGET_DB_NAME}",
  "connection.username": "${TARGET_DB_USER}",
  "connection.password": "${TARGET_DB_PASSWORD}",
  "insert.mode": "upsert",
  "primary.key.mode": "record_key",
  "schema.evolution": "basic",
  "table.name.format": "cdc_landing.customers",
  "delete.enabled": "true"
}
